[package]
name = "liquid-sys"
edition = "2021"
version = "0.1.0"
links = "liquid"
build = "build.rs"

[build-dependencies]
cmake = "0.1"
pkg-config = "0.3"
bindgen = "0.69"

[dependencies]
libc = "0.2"
num-complex = "0.4.6"
